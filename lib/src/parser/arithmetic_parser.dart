/// Arithmetic expression parser.
///
/// Port of `parser/arithmetic-parser.ts` (+ `arithmetic-primaries.ts`) in
/// progress. Part of the `parser` library.
//
// TODO(dbash): port the Pratt-style arithmetic parser. Placeholder until then.
part of 'parser.dart';

/// Parse the arithmetic expression in [input].
ArithmeticExpressionNode parseArithmeticExpressionImpl(Parser p, String input) {
  throw UnimplementedError(
    'parseArithmeticExpression: arithmetic parser not yet ported',
  );
}

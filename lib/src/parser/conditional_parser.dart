/// Conditional expression parser (for `[[ ]]`).
///
/// Port of `parser/conditional-parser.ts` in progress. Part of the `parser`
/// library.
//
// TODO(dbash): port the [[ ]] expression grammar. Placeholder until then.
part of 'parser.dart';

/// Parse a conditional expression for `[[ ... ]]`.
ConditionalExpressionNode parseConditionalExpression(Parser p) {
  throw UnimplementedError(
    'parseConditionalExpression: conditional parser not yet ported',
  );
}

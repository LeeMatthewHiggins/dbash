# Attribution

`dbash` is a pure Dart port of **just-bash** by Vercel Labs.

- Upstream project: https://github.com/vercel-labs/just-bash
- Upstream license: Apache-2.0 (as stated in the upstream package README)

This package is a derivative work. The bash lexer, parser, AST, interpreter,
virtual filesystem design, and test cases are ported from the upstream
TypeScript implementation. Original copyright belongs to the upstream authors.

A reference copy of the upstream source is kept in `legacy-source/` for porting
purposes and is excluded from version control and package publication.
